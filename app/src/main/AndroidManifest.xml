<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Единственное, что нужно приложению от системы: сеть и право держать
         соединение живым. Ни контактов, ни хранилища, ни геолокации. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="false"
        android:usesCleartextTraffic="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Obsidian.Theme">

        <!-- allowBackup="false" не случайность: автобэкап Google утащил бы
             зашифрованную базу вместе с ключами на чужие серверы.

             usesCleartextTraffic="false" — тоже не формальность. Ядро и так
             ходит только по wss://, но флаг переносит это правило из кода в
             систему: с ним обычный http перестаёт быть возможным вообще, а не
             просто не используемым. Стоит однажды появиться отладочному
             адресу без TLS — приложение откажет, а не отправит. -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustNothing">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".ObsidianService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
    </application>
</manifest>
